# URL of the LuxuryCuts API (include the /api suffix)
VITE_API_URL=http://localhost:4000/api

# Base path the site is served from. Use / locally; the GitHub Pages
# workflow sets this to /LuxuryCuts/ automatically.
VITE_BASE_PATH=/
