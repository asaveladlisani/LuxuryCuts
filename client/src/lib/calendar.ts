export type CalendarBooking={service:string;barber:string;date:string;time:string;duration:number;name:string;email:string;phone:string};
function startDate(b:CalendarBooking){const [y,m,d]=b.date.split('-').map(Number);const [h,min]=b.time.split(':').map(Number);return new Date(y,m-1,d,h,min)}
function pad(n:number){return String(n).padStart(2,'0')}
function icsDate(d:Date){return `${d.getFullYear()}${pad(d.getMonth()+1)}${pad(d.getDate())}T${pad(d.getHours())}${pad(d.getMinutes())}00`}
export function googleCalendarUrl(b:CalendarBooking){const s=startDate(b),e=new Date(s.getTime()+b.duration*60000);const p=new URLSearchParams({action:'TEMPLATE',text:`LuxuryCuts — ${b.service}`,dates:`${icsDate(s)}/${icsDate(e)}`,details:`Appointment with ${b.barber}. Booked for ${b.name}.\nPhone: ${b.phone}`,location:'21 Bree Street, Cape Town, South Africa'});return `https://calendar.google.com/calendar/render?${p}`}
export function downloadICS(b:CalendarBooking){const s=startDate(b),e=new Date(s.getTime()+b.duration*60000);const body=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//LuxuryCuts//Booking//EN','BEGIN:VEVENT',`UID:${Date.now()}@luxurycuts.local`,`DTSTART:${icsDate(s)}`,`DTEND:${icsDate(e)}`,'SUMMARY:LuxuryCuts — '+b.service,`DESCRIPTION:Appointment with ${b.barber} for ${b.name}. Phone: ${b.phone}`,'LOCATION:21 Bree Street\\, Cape Town\\, South Africa','END:VEVENT','END:VCALENDAR'].join('\r\n');const blob=new Blob([body],{type:'text/calendar;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='luxurycuts-appointment.ics';a.click();URL.revokeObjectURL(a.href)}
