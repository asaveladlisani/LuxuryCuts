import express from 'express';import cors from 'cors';import serviceRoutes from './routes/service.routes.js';import barberRoutes from './routes/barber.routes.js';import bookingRoutes from './routes/booking.routes.js';import './db/database.js';
const app=express();app.use(cors(process.env.CORS_ORIGIN?{origin:process.env.CORS_ORIGIN.split(',')}:undefined));app.use(express.json());app.get('/api/health',(_,res)=>res.json({status:'ok',service:'LuxuryCuts API'}));app.use('/api/services',serviceRoutes);app.use('/api/barbers',barberRoutes);app.use('/api/bookings',bookingRoutes);const port=Number(process.env.PORT||4000);app.listen(port,()=>console.log(`LuxuryCuts API running on http://localhost:${port}`));
